# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib
*.env
*.tar

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

# Dependency directories (remove the comment below to include it)
vendor/


.idea
setup_test.sh
Makefile
deploy
test
tmp
local

#testing
*.local

# Terraform state files and backups
*.tfstate
*.tfstate.*

# Terraform local cache and plugins
.terraform/

# Crash log files
crash.log

# Terraform sensitive variable definitions (if you use them, though not recommended for secrets)
*.tfvars
*.tfvars.json

# Override files (used for local development overrides)
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Provider-specific cached files (e.g., for some providers)
.terraform.d/

# CLI configuration (if placed in the project root, though usually in ~/.terraformrc)
.terraformrc
